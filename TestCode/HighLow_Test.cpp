#include <iostream>
#include </home/josh/GCRepo/roboio/Include/roboard.h>
//#include <unistd.h>

using namespace std;
/*
int main(){

int val = 11;
int choice;
roboio_SetRBVer(RB_100RD);
rcservo_Init(RCSERVO_USENOPIN);

while(choice != 999){

cout << "High or Low menu?(1 or 0)\n";
cin >> choice;

if (choice == 1){
    cout << "Enter pin to bring high (99 to quit)\n";
    cin >> choice;

    while (choice != 99){
        rcservo_OutPin(choice,1);
        cout << "Enter pin to bring high (99 to quit)\n"; 
        cin >> choice;
    }
}
else if (choice == 0){
    cout << "Enter pin to bring low (99 to quit)\n";
    cin >> choice;

    while (choice != 99){
        rcservo_OutPin(choice,0);
        cout << "Enter pin to bring low (99 to quit)\n";
        cin >> choice;
    }
}


}

rcservo_Close();
return 0;
}
*/
